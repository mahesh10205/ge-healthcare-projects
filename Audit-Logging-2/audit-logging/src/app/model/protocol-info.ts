export class ProtocolInfo {

    protocolNum: number;

    protocolText: string;

    constructor(protocol: number, text: string) {
        this.protocolNum = protocol;
        this.protocolText = text;
    }
}